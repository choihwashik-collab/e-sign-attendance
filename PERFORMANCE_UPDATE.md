# 속도 개선 (API 14)

## 반영 내용

- 서버 결과를 POST 응답으로 직접 수신: 정상 경로에서 작업당 POST 1회. 응답을 브라우저가 읽지 못하면 같은 요청 번호의 확인 응답만 조회하며 저장 요청을 재전송하지 않습니다.
- 관리자 로그인 응답에 전체 현황과 기본 설정 포함. 구버전 API 13에서는 기존 조회 방식으로 호환됩니다.
- 연수 설정만 저장할 때 참석자 시트를 재작성하지 않습니다. 기존 명단 수정 요청에서도 서명 PNG의 재업로드를 생략합니다. 신규 자료 가져오기는 이미지를 유지합니다.
- 기본명단 30초, 전체 현황 15초, 이미 연 연수 10초의 메모리 재사용. 저장 시 연수 현황 재조회가 가능하도록 무효화합니다.
- 공개 상세 조회 결과는 버전별로 최대 5초 캐시. 접수 종료 확인은 캐시보다 먼저 실행하며 서명 원본/상세 사유는 공개하지 않습니다.
- 공개 조회의 기존 7초 대기 한도를 20초로 조정하고 무응답일 때 읽기만 한 번 재시도합니다. 실제 기존 서버의 상세 응답 12.7초가 확인됐습니다. 대기 한도 증가는 실패 감소용이며 자체 속도 향상 수치가 아닙니다.
- 사용자 작업은 즉시 처리 중 안내 및 중복 실행 방지. 자동 갱신은 화면을 잠그지 않으며, 갱신 중 시작한 편집/서명 및 새 저장 결과를 덮지 않습니다.

## 적용

Google Apps Script의 Code.gs를 교체하고 저장한 뒤 배포 관리에서 기존 배포 수정 → 새 버전 → 배포합니다. ADMIN_KEY나 /exec 주소는 변경하지 않습니다. ping의 apiVersion이 14이면 적용 완료입니다.

프런트엔드는 API 13과 호환되지만 POST 직접 응답, 통합 로그인, 설정만 저장 등 서버 개선 효과는 API 14 배포 후 적용됩니다. 실제 개선 후 초 단위 시간은 운영 서버 재배포 뒤 측정해야 합니다.

## 확인

`node --test tests/security.test.cjs tests/export.test.cjs`

직접 응답 1회, CORS 실패 후 중복 POST 방지, 조회 재시도, 설정 수정 시 서명 유지, 권한 및 출력 테스트를 포함합니다. 휴대폰 자체의 네트워크 차단 여부는 이 테스트로 판단할 수 없습니다.
