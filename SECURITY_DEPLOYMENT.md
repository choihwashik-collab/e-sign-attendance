# 보안 변경 및 운영 전 필수 작업 (v10)

## 현재 상태

- 이 배포에는 웹 화면과 서버 소스 변경이 포함됩니다.
- GitHub 코드 변경만으로 실제 Google Apps Script 서버는 업데이트되지 않습니다. 아래 서버 재배포를 반드시 완료하세요.
- 따라서 기존 배포의 무인증 API는 운영자가 업데이트하거나 중지하기 전까지 그대로입니다.
- 실서비스 침투 테스트, 실제 휴대폰 ↔ Google 서버 저장 왕복, 동시 접속 부하 테스트는 미완료입니다. 이 문서는 보안 보증서가 아닙니다.

## 운영자가 직접 할 일

### 1. 기존 자료 백업 및 점검 중 접수 중지

1. Google Sheets에서 전체 스프레드시트 사본을 만듭니다. 숨김 시트도 포함해야 합니다.
2. 기존 서명부 PDF도 별도 보관합니다. 백업에는 개인정보와 서명 원본이 있으므로 공개 폴더에 두지 마세요.
3. 점검·배포 시간에는 참석자에게 접수를 중지한다고 알립니다.
4. 기존 Apps Script 배포가 여러 개라면 목록을 확인합니다. 보호되지 않은 구버전 배포는 보관처리하여 접근을 중지해야 합니다. 새 배포 하나를 추가하는 것만으로 구버전의 취약점이 닫히지 않습니다.

### 2. 서버 코드 적용 및 관리자 키 발급

1. 현재 출석 시트의 **확장 프로그램 → Apps Script**를 엽니다.
2. 프로젝트의 기존 코드를 백업한 후 이 프로젝트의 **google-apps-script/Code.gs 전체**로 교체합니다.
3. 편집기에서 **initializeSecurity** 함수를 한 번 실행합니다.
4. Google 권한 승인 화면에서 본인의 출석 시트 프로젝트인지 확인한 뒤 직접 승인합니다.
5. **프로젝트 설정 → 스크립트 속성 → ADMIN_KEY**의 값을 확인하여 비밀번호 관리자 등 안전한 곳에 보관합니다.
   - 키는 소스 파일, GitHub, QR, 이메일 게시판, 이 채팅에 붙이지 마세요.
   - 기존의 기본 관리자/마스터 비밀번호는 더 이상 작동하지 않습니다.
   - 키 변경은 스크립트 속성에서 32자 이상의 새 무작위 값으로 교체합니다. 재발급 시 기존 키는 서버에서 즉시 거부됩니다.
6. **배포 → 배포 관리 → 해당 배포 편집 → 새 버전 → 배포**로 반영합니다.
   - 소유자 권한으로 실행합니다.
   - 로그인 없는 참석자 QR을 유지하려면 진입점은 '모든 사용자'여야 합니다. 데이터 권한은 새 코드의 관리자 키와 연수별 초대 토큰으로 검사합니다.
   - 이 공개 진입점은 Google 계정 본인 인증을 제공하지 않습니다. 조직 정책상 공개 웹 앱이 금지되어 있으면 조직 로그인 기반 서버로 전환해야 합니다.
7. 더 이상 쓰지 않는 **모든 구버전 배포**를 보관처리합니다. 코드 저장만으로 배포 버전이 바뀌지는 않습니다.

키 발급·Google 권한 승인·배포 변경은 사용자 계정의 작업이므로 이번 작업에서 대신 실행하지 않았습니다.

### 3. 웹 화면 연결

1. 변경된 index.html, js 폴더, css 폴더를 기존 웹 호스팅/GitHub Pages에 반영합니다.
2. 특히 새로 생성된 **css/tailwind.css**를 빠뜨리지 마세요. 별도 Node 설치 없이 배포 파일만으로 화면이 표시됩니다.
3. 브라우저를 강력 새로고침합니다.
4. **진행자 로그인 / 모드 선택**에서 배포된 /exec 주소와 ADMIN_KEY를 입력합니다.
5. 기존 서버 묶음을 열어 이름·인원·서명·출력 내용을 확인합니다.
6. 관리 화면의 QR 탭에서 **24시간 QR 발급**을 누릅니다. 기존 QR과 기존 링크는 사용할 수 없습니다.

로컬 자료를 서버로 옮길 때만 **기존 로컬 자료 → 서버 복사**를 사용하세요. 선택한 자료를 새 묶음으로 명시적으로 업로드하며 원본은 유지합니다. 반복 실행하면 중복 묶음이 생길 수 있습니다. 기존 서버 자료를 재등록할 필요는 없습니다.

### 4. 실제 서비스에서 통과해야 할 확인

실제 명단 대신 테스트용 묶음과 가상 인물로 확인하세요.

- 로그인하지 않은 브라우저는 전체 연수 목록과 서명 원본을 볼 수 없어야 합니다.
- 잘못된 관리자 키로 로그인할 수 없어야 합니다.
- 휴대폰 QR 접속 → 명단 조회 → 가상 서명 제출 → 관리자 화면 및 Google 시트에 반영되는지 확인합니다.
- 휴대폰 인터넷을 끊고 제출하면 완료 화면 대신 오류가 표시되고 입력한 서명이 남아야 합니다.
- 두 브라우저가 같은 자료를 열고 한쪽이 변경한 뒤 다른 쪽이 오래된 명단을 저장하면 충돌 오류가 나야 합니다.
- 서명 후 QR을 발급해도 기존 서명이 유지되어야 합니다.
- 새 QR 발급 또는 접수 종료 후 이전 QR로 조회·서명할 수 없어야 합니다.
- PDF/Excel 출력과 서명 이미지 품질을 확인합니다.
- 구버전 배포 주소도 확인하여 미인증 조회가 더 이상 가능하지 않은지 확인합니다.

## 달라지는 사용 방법과 제약

| 변경 | 사용상 영향 |
|---|---|
| 서버 관리자 키 도입 | 진행자는 긴 키가 필요하며 새로고침 후 다시 로그인합니다. 키는 페이지 메모리에만 있습니다. |
| 공개 연수 목록과 QR | 일반 앱 주소에서 접수 중인 연수 목록을 볼 수 있습니다. QR은 특정 연수로 바로 이동하는 편의 기능이며 본인 인증 수단이 아닙니다. ‘접수 종료’를 누르면 공개 목록과 제출이 차단됩니다. |
| 이름·소속만 참석자에게 제공 | 다른 사람의 서명 원본·병가 등 불참 사유는 참석자에게 내려주지 않습니다. 같은 QR의 참석자는 해당 연수의 이름과 부서는 볼 수 있습니다. |
| 현장 등록은 진행자 전용 | 명단에 없는 사람은 진행자에게 등록을 요청해야 합니다. |
| 저장 확인 후 완료 | 인터넷이 끊기면 원격 서명 완료를 표시하지 않습니다. 재연결 후 같은 서명으로 재시도하세요. |
| 동시 수정 충돌 감지 | 충돌 시 최신 자료를 불러온 뒤 수정해야 합니다. 입력 중인 설정은 먼저 저장해야 합니다. |
| 30초 주기 갱신 | 백그라운드 탭, 설정 입력 중, 서명 중에는 자동 갱신하지 않습니다. 즉각 반영이 필요하면 수동으로 최신 자료를 불러오세요. |
| 한 묶음 최대 200명·연수 30개 | 큰 행사는 여러 묶음으로 나누세요. 실제 동시 수용 인원을 보장하는 수치는 아닙니다. |
| 명단 파일 5MB·PDF 50페이지 제한 | 큰 파일은 분할해야 합니다. 새 PDF 모듈은 최신 브라우저와 인터넷이 필요합니다. 구형 기기에서는 TXT/Excel을 사용하세요. |
| 서명 이미지 축소 | 새 서명은 PNG 16,000자 이내로 축소됩니다. 복잡한 서명은 다시 입력해야 할 수 있으며 출력 품질 확인이 필요합니다. 기존 서버 서명은 임의 축소하지 않습니다. |
| 단일 기기 모드 | 별도 로그인 보호 없이 같은 브라우저에만 저장됩니다. 휴대폰 공유가 안 되며 공용 PC에는 부적합합니다. 자료·서명은 브라우저 데이터 삭제 시 사라지므로 JSON 백업이 필요합니다. |

## 여전히 남는 위험

- **본인 확인과 공개 범위:** 앱 주소를 아는 사람은 접수 중인 연수명, 일정, 참석자 이름·소속·처리 여부를 보고 다른 사람 이름으로 제출할 수 있습니다. 서명 이미지와 서버 시각은 전자서명 인증서/신원 인증이 아닙니다. 상세 비고와 서명 원본은 공개 응답에서 제외됩니다. 대리서명 또는 명단 공개 방지가 필수라면 조직 계정 로그인이나 개인별 인증코드가 필요합니다.
- **서버 할당량:** 공개 Apps Script 진입점에 대한 요청 폭주를 완전히 차단하지 못합니다. Google 할당량과 실행 지연, 캐시 조기 삭제로 확인 응답을 받지 못할 수 있습니다. 오류 시 저장 여부를 확인하지 않고 반복 생성·수정하지 마세요.
- **저장 결과 확인:** POST 후 임의의 256비트 요청 번호로 90초짜리 확인 응답을 조회합니다. 관리자 키와 초대 토큰은 POST 본문에만 전송합니다. 확인 응답에는 조회 결과가 포함될 수 있으므로 네트워크 로그도 공개하지 마세요.
- **데이터 보관:** 원격 자료를 새로 브라우저 영구 저장소에 저장하지 않습니다. 그러나 구버전의 로컬 자료와 다운로드한 백업은 자동 삭제하지 않습니다. 진행자 PC, 시트 공유 권한, 백업 접근 권한도 보호해야 합니다.
- **자동 삭제:** 기존 setupCleanupTrigger로 설치한 30일 정리 기능은 남아 있습니다. 이번에 자동으로 실행하거나 설치하지 않았습니다. 보관 기간은 기관 규정에 맞춰 직접 결정해야 합니다. 이미 설치했다면 **생성일 기준 30일**임을 확인하세요. 휴대폰 QR 만료와 자료 삭제는 별개입니다.
- **수동 시트 편집:** 충돌 감지는 앱을 통한 변경에 적용됩니다. 운영 중 Google 시트를 직접 수정하면 앱의 버전 번호와 맞지 않을 수 있으므로 피하세요.
- **외부 자원:** 일부 PDF/Excel/QR 라이브러리와 글꼴은 인터넷 CDN에서 받습니다. 실행 스크립트는 버전 고정 및 가능한 곳에 무결성 검사(SRI)를 적용했지만 완전 오프라인 앱은 아닙니다.
- **복구:** Google Sheets는 트랜잭션 데이터베이스가 아닙니다. 서버 오류에 대한 완전한 원자적 복구·감사 이력·침투 테스트는 제공하지 않습니다. 중요한 행사에는 사전 백업과 별도 출석 확인 수단을 준비하세요.

## 기능 및 복구

기본명단은 관리자만 저장·수정하며 최대 30개, 각 200명입니다. 연수에 적용한 복사본은 독립적입니다. 성명·소속을 바꾸면 인원 교체로 처리하여 이전 서명을 비웁니다. 참석자는 로그인 없이 같은 이름을 재선택해 재서명할 수 있습니다. 조퇴·출장 등은 비고에 사유를 저장하고 PDF/Excel 서명란은 공란입니다. Excel에는 실제 PNG 그림이 포함됩니다.

코드 백업 브랜치: `backup/before-security-rosters-2026-09-08`, 원본 커밋 `09fe06ff6878adc6b72b9e048196baa0a1e5d822`. 코드 복구와 Google 시트 데이터 복구는 별개입니다. 구버전 코드로 복구하면 기존 보안 취약점도 돌아오므로 공개 운영 상태에서 되돌리지 마세요.

## 개발 검증

실행: `node --test tests/security.test.cjs`

테스트는 모의 Google Sheets/Apps Script 서비스와 브라우저 객체를 사용하며 실제 서버와 통신하거나 사용자 자료를 변경하지 않습니다. 서버 인증, 역할 제한, QR 만료·회전·종료, 덮어쓰기 방지, 서명 재시도, XSS 출력 이스케이프, 수식 입력 방어, 오류 표시 등을 검증합니다.

CSS 수정 후 재생성: `npx --yes tailwindcss@3.4.17 -i ./css/tailwind-input.css -o ./css/tailwind.css --minify`

라이브러리 참고: [PDF.js 보안 공지](https://github.com/mozilla/pdf.js/security/advisories/GHSA-hq66-cqwq-w95j), [SheetJS 설치 안내](https://docs.sheetjs.com/docs/getting-started/installation/standalone/), [jsPDF 릴리스](https://github.com/parallax/jsPDF/releases/tag/v4.2.1).

